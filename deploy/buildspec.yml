version: 0.2

phases:
  install:
    runtime-versions:
      python: 3.7
    commands:
      - python -v
      - ls
  build:
    commands:
      - zip -r cloudwatch_to_slack.zip cloudwatch_lambda
      - ls
      - echo "Success"
      - which aws
      - aws s3 ls